server:
  port: 8091

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nursery
    username: josemaria
    password: maria17
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
