package org.fdryt.ornamental.security.nursery;
/*

import com.google.common.collect.Sets;

import java.util.Set;

import static org.fdryt.ornamental.security.nursery.NurseryUserPermission.CREATE_PLANT;

public enum NurseryUserRole {

    ASSISTANT(Sets.newHashSet(CREATE_PLANT));

    private final Set<NurseryUserPermission> permission;

    NurseryUserRole(Set<NurseryUserPermission> permission) {
        this.permission = permission;
    }

    public Set<NurseryUserPermission> getPermission() {
        return permission;
    }

    */
/*public Set<SimpleGrantedAuthority> getGrantedAuthorities() {
        Set<SimpleGrantedAuthority> permissions = getPermission().stream()
                .map(permission -> new SimpleGrantedAuthority(permission.getPermission()))
                .collect(Collectors.toSet());

        permissions.add(new SimpleGrantedAuthority("ROLE_" + this.name()));
        return permissions;
    }*//*

}
*/
